#include "main.h"
#include <string.h>

/**
 * strcat - concatenates two strings
 *
 * @dest: input value
 * @src: string
 *
 * Return: dest
*/

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
